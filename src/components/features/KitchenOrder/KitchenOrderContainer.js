import { connect } from 'react-redux'
import KitchenOrder from './KitchenOrder';
import {  } from '../../../redux/kitchenRedux';

const mapStateToProps = (state) => ({
  
});

export default connect(mapStateToProps)(KitchenOrder);